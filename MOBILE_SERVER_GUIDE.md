# دليل تشغيل خادم الواتساب على الهواتف الذكية (Android / iOS) 📱

يمكنك تشغيل واستخدام خادم الواتساب من الهواتف بطريقتين:

---

## 🟢 الطريقة الأولى: تشغيل السيرفر بالكامل داخل هاتف أندرويد (Stand-alone Android Server)

تحويل أي هاتف أندرويد (حتى الهواتف القديمة) إلى **سيرفر واتساب دائم يعمل 24/7** على شبكة الـ Wi-Fi عبر تطبيق **Termux**.

### الخطوات:
1. قم بتحميل وتثبيت تطبيق **Termux** على الهاتف (يُفضل من متجر F-Droid أو GitHub).
2. افتح تطبيق Termux واكتب الأمر التالي لتجهيز البيئة وتثبيت Node.js و Chromium:
   ```bash
   pkg update -y && pkg upgrade -y
   pkg install -y nodejs-lts chromium git
   ```
3. انسخ مجلد `whatsapp-server` إلى الهاتف، أو قم بتنزيل ملفاته داخل Termux.
4. ادخل إلى مجلد السيرفر وشغّل السكريبت الجاهز:
   ```bash
   cd whatsapp-server
   export PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
   export PUPPETEER_EXECUTABLE_PATH=$(which chromium)
   npm install
   node index.js
   ```
5. سيعمل السيرفر فوراً على هاتف الأندرويد على المنفذ `3001` (مثال: `http://192.168.1.50:3001`).

---

## 🔵 الطريقة الثانية: استخدام السيرفر من أي هاتف (آيفون / أندرويد) عبر متصفح الهاتف

إذا كان السيرفر يعمل على الكمبيوتر (أو على هاتف أندرويد مخصص):

1. افتح متصفح الهاتف (Safari على الآيفون أو Chrome على الأندرويد).
2. ادخل على رابط الموقع المحلي أو عنوان السيرفر (مثال: `http://192.168.1.15:3000` أو صفحة الأدمين `/admin/whatsapp`).
3. ستتمكن من مسح الـ QR Code مباشرة من شاشة الهاتف واستخدام جميع خدمات الواتساب بسهولة.
